name: counter
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive_plus_secure: ^1.1.14
  isar_plus: ^1.1.1
  isar_plus_flutter_libs: ^1.1.1
  
dev_dependencies:
  build_runner: any
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
